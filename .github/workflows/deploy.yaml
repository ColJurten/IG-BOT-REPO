name: Build and Deploy Docker Images

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push ig-automation
        uses: docker/build-push-action@v5
        with:
          context: ./ig-automation
          file: ./ig-automation/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/ig-automation:latest

      - name: Build and push igbot (replace with your server build context if needed)
        uses: docker/build-push-action@v5
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/igbot:latest